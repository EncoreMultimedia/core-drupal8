{
  "name": "base-encore",
  "scripts": {
    "install": "cd web/core && yarn && cd ../themes/main && yarn",
    "start": "cd web/themes/main && yarn start",
    "build": "cd web/themes/main && yarn build"
  }
}
